{
  "version": 2,
  "buildCommand": "cd frontend && npm run build && cd .. && npm run build:server",
  "installCommand": "npm install && cd frontend && npm install",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}
  